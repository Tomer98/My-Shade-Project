{
  "name": "my-shade-project",
  "version": "1.0.0",
  "description": "Smart Shade Automation - Full Stack Project",
  "scripts": {
    "setup": "cd server && npm install && cd ../client && npm install",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  }
}